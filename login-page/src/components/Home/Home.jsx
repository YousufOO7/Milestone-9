import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div>
            <h2 className="text-4xl">This is home page</h2>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;