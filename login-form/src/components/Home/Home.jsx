import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div>
            <h2 className="text-4xl">this is a home page with auth</h2>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;